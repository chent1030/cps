<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.cps.mapper.CpsInitialReviewEventMapper">

    <insert id="insert" parameterType="com.company.cps.domain.CpsInitialReviewEvent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cps_initial_review_event (
          task_id, issue_id, version_no, event_type, detail, operator_emp_no, created_at
        ) VALUES (
          #{taskId}, #{issueId}, #{versionNo}, #{eventType}, #{detail}, #{operatorEmpNo}, #{createdAt}
        )
    </insert>

    <select id="findByTaskId" resultType="com.company.cps.domain.CpsInitialReviewEvent">
        SELECT * FROM cps_initial_review_event WHERE task_id = #{taskId} ORDER BY id ASC
    </select>

    <select id="findByIssueId" resultType="com.company.cps.domain.CpsInitialReviewEvent">
        SELECT * FROM cps_initial_review_event WHERE issue_id = #{issueId} ORDER BY id ASC
    </select>
</mapper>
