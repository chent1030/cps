<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.cps.mapper.CpsKnowledgeCaseMapper">

    <select id="findEnabledById" resultType="com.company.cps.domain.CpsKnowledgeCase">
        SELECT *
        FROM cps_knowledge_case
        WHERE id = #{id}
          AND enabled = 1
    </select>

    <select id="findById" resultType="com.company.cps.domain.CpsKnowledgeCase">
        SELECT *
        FROM cps_knowledge_case
        WHERE id = #{id}
    </select>
    <select id="findByCategoryL2Id" resultType="com.company.cps.domain.CpsKnowledgeCase">
        SELECT * FROM cps_knowledge_case WHERE category_l2_id = #{categoryL2Id} ORDER BY id ASC LIMIT 1
    </select>

    <select id="findEnabledByIds" resultType="com.company.cps.domain.CpsKnowledgeCase">
        SELECT *
        FROM cps_knowledge_case
        WHERE enabled = 1
          AND id IN
          <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
          </foreach>
    </select>

    <select id="findAll" resultType="com.company.cps.domain.CpsKnowledgeCase">
        SELECT *
        FROM cps_knowledge_case
        <where>
            <if test="enabled != null">
                enabled = #{enabled}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <sql id="adminFilter">
        <where>
            <if test="enabled != null">
                enabled = #{enabled}
            </if>
            <if test="category != null and category != ''">
                AND (
                    category_l1_name LIKE CONCAT('%', #{category}, '%')
                    OR category_l2_name LIKE CONCAT('%', #{category}, '%')
                )
            </if>
        </where>
    </sql>

    <select id="findAdminPage" resultType="com.company.cps.domain.CpsKnowledgeCase">
        SELECT kc.*, image.file_url AS image_url, image.reason, image.measure, image.milvus_vector_id, image.vector_retry_count, image.vector_updated_at, image.vector_status, image.vector_error_msg
        FROM cps_knowledge_case kc
        LEFT JOIN cps_knowledge_case_image image ON image.id = (SELECT MIN(i2.id) FROM cps_knowledge_case_image i2 WHERE i2.case_id = kc.id)
        <include refid="adminFilter"/>
        ORDER BY updated_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findAdminExport" resultType="com.company.cps.domain.CpsKnowledgeCase">
        SELECT kc.*, image.file_url AS image_url, image.reason, image.measure, image.milvus_vector_id, image.vector_retry_count, image.vector_updated_at, image.vector_status, image.vector_error_msg
        FROM cps_knowledge_case kc
        LEFT JOIN cps_knowledge_case_image image ON image.id = (SELECT MIN(i2.id) FROM cps_knowledge_case_image i2 WHERE i2.case_id = kc.id)
        <include refid="adminFilter"/>
        ORDER BY updated_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <select id="countAdmin" resultType="long">
        SELECT COUNT(1)
        FROM cps_knowledge_case
        <include refid="adminFilter"/>
    </select>

    <insert id="upsert" parameterType="com.company.cps.domain.CpsKnowledgeCase" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cps_knowledge_case (
            id,
            category_l1_id,
            category_l2_id,
            category_l1_name,
            category_l2_name,
            enabled,
            created_by
        ) VALUES (
            #{id},
            #{categoryL1Id},
            #{categoryL2Id},
            #{categoryL1Name},
            #{categoryL2Name},
            #{enabled},
            #{createdBy}
        )
        ON DUPLICATE KEY UPDATE
            category_l1_id = VALUES(category_l1_id),
            category_l2_id = VALUES(category_l2_id),
            category_l1_name = VALUES(category_l1_name),
            category_l2_name = VALUES(category_l2_name),
            enabled = VALUES(enabled),
            updated_at = NOW()
    </insert>

    <update id="setEnabled">
        UPDATE cps_knowledge_case
        SET enabled = #{enabled},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
