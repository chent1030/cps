<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.cps.mapper.CpsCheckItemMapper">

    <sql id="BaseColumns">
        id, item_code, content, photo_category, deduct_score, status,
        applicable_room_types, config_version, enabled, created_by, updated_by, created_at, updated_at
    </sql>

    <select id="findAll" resultType="com.company.cps.domain.CpsCheckItem">
        SELECT <include refid="BaseColumns" />
        FROM cps_check_item
        WHERE 1 = 1
        <if test="photoCategory != null and photoCategory != ''">
          AND photo_category = #{photoCategory}
        </if>
        <if test="status != null and status != ''">
          AND status = #{status}
        </if>
        <if test="enabled != null">
          AND enabled = #{enabled}
        </if>
        ORDER BY config_version DESC, item_code ASC
    </select>

    <select id="findById" resultType="com.company.cps.domain.CpsCheckItem">
        SELECT <include refid="BaseColumns" />
        FROM cps_check_item
        WHERE id = #{id}
    </select>

    <select id="findByCode" resultType="com.company.cps.domain.CpsCheckItem">
        SELECT <include refid="BaseColumns" />
        FROM cps_check_item
        WHERE item_code = #{itemCode}
    </select>

    <insert id="insert" parameterType="com.company.cps.domain.CpsCheckItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cps_check_item (
            item_code, content, photo_category, deduct_score, status,
            applicable_room_types, config_version, enabled, created_by, updated_by
        ) VALUES (
            #{itemCode}, #{content}, #{photoCategory}, #{deductScore}, #{status},
            #{applicableRoomTypes}, 1, #{enabled}, #{createdBy}, #{updatedBy}
        )
    </insert>

    <update id="update" parameterType="com.company.cps.domain.CpsCheckItem">
        UPDATE cps_check_item
        SET content = #{content},
            photo_category = #{photoCategory},
            deduct_score = #{deductScore},
            status = #{status},
            applicable_room_types = #{applicableRoomTypes},
            config_version = config_version + 1,
            enabled = #{enabled},
            updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <update id="setEnabled">
        UPDATE cps_check_item
        SET enabled = #{enabled}, updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>
</mapper>
