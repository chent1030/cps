# Production deployments should normally use a same-origin reverse proxy.
VITE_API_BASE_URL=/api
# Local Vite development proxy target.
VITE_BACKEND_URL=http://localhost:8080
# Optional corporate SSO entry point used when the API returns 401.
VITE_SSO_LOGIN_URL=
